-- UNION
SELECT 1 AS ID
UNION
SELECT 1 AS ID

-- UNION ALL
SELECT 1 AS ID
UNION ALL
SELECT 1 AS ID

-- EXCEPT
; WITH cte_Test
AS
(
	SELECT 1 AS ID
	UNION
	SELECT 2
)
SELECT ID FROM cte_Test
EXCEPT
SELECT 3

-- INTERSECT - Example 1
; WITH cte_Test
AS
(
	SELECT 1 AS ID
	UNION
	SELECT 2
)
SELECT ID FROM cte_Test
INTERSECT
SELECT 3

-- INTERSECT - Example 2
; WITH cte_Test
AS
(
	SELECT 1 AS ID
	UNION
	SELECT 2
)
SELECT ID FROM cte_Test
INTERSECT
SELECT 1